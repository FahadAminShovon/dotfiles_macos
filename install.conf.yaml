- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.zshenv:
    ~/.gitconfig:
    ~/.zprofile:
    ~/.p10k.zsh: p10k.zsh
    ~/.config/bat:
      create: true
      path: config/bat
    ~/.config/kitty:
      create: true
      path: config/kitty

- create:
    - ~/Workspace
    - ~/.config/zsh

- shell:
    - command: ./setup_homebrew.zsh
      stdin: true
      stdout: true
      stderr: true
    - command: ./setup_zsh.zsh
      stdin: true
      stdout: true
      stderr: true
    - command: ./setup_node.zsh
      stdin: true
      stdout: true
      stderr: true
    # Copy fonts
    - command: cp -a fonts/. ~/Library/Fonts/
      stderr: true
